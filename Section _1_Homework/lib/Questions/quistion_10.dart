void main() {
  employeeName = "Ali";
  print(employeeName);

  String employeeName = "Ali";

  /*
  if you try to use a variable before it is declared, an error occor.
  Because the Dart compile begins by reading the code line by line in order,
  therefore it doesn't know what you want to use? ,
  it doesn't know the type, location, or value of the variable, so it cannot proceed.
  
   */
}
