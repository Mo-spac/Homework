void main() {
  print("""
      The main function is the entry point of a Dart application.
      It’s the first function that gets executed when the program runs,
      and it’s where the program’s execution begins.
      Every Dart application must have a main function for it to run.
      """);
}
